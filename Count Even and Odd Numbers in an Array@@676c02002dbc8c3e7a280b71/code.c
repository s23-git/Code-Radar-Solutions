#include<stdio.h>
int main(){
    int arr[N] = {};
    scanf("%d", &arr[N]);

    if(arr[N] % 2){
        printf("%d", arr[N]);
    } else{
        printf("%d", arr[N]);
    }
    return 0;
}
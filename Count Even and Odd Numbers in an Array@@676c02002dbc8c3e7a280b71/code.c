#include <stdio.h>

int main() {
    int arr;
    scanf("%d", &arr);

    if (arr % 2 != 0) {  // If arr is odd
        printf("%d\n", arr);
    } else {  // If arr is even
        printf("%d\n", arr);
    } 

    return 0;
}

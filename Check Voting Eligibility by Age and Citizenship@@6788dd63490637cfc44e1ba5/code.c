#include <stdio.h>
int main(){
    int age=1, citizen_status=0;
    scanf("%d %d", &age, &citizen status);

    if(age>18 && citizen_status){
        printf("Eligible\n");
    } else if(){
        printf("Not eligible");
    }
    return 0;
}
#include <stdio.h>
int main(){
    int age=1, citizen status=0;
    scanf("%d %d", &age, &citizen status);

    if(age>18 && citizen status){
        printf("Eligible\n");
    } else if(){
        printf("Not eligible");
    }
    return 0;
}
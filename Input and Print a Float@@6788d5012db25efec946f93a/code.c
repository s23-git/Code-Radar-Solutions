#include <stdio.h>

int main() {
    float f;

    printf("Enter a single floating point number: ");
    scanf("%f", &f);

    printf("You entered a single floating point number: %f\n", f);
    return 0;
}
#include<stdio.h>
    void sort(int arr[], int n);

    int main(){
        int arr[];
        scanf("%d", arr);
    }
    void sort(int arr[], int n);
    for(int i=0; i<n; i++){
        printf("%d", arr[i]);
        if(array is non-decreasing){
            printf("Sorted\n");
        } else{
            printf("Not Sorted\n");
        }
        return 0;
    }
